{
  "name": "create-selector",
  "description": "Wrapper for reselect to allow deferring creation of selectors.",
  "version": "4.0.2",
  "author": "Henrik Joreteg <henrik@joreteg.com> (joreteg.com)",
  "dependencies": {
    "reselect": "3.0.1"
  },
  "devDependencies": {
    "microbundle": "0.11.0",
    "nodemon": "1.18.10",
    "standard": "12.0.1",
    "tape": "4.10.1"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.m.js",
  "prettier": {
    "singleQuote": true,
    "semi": false
  },
  "scripts": {
    "build": "standard --fix && microbundle --no-compress",
    "test": "npm run build && node test.js",
    "test-watch": "nodemon test.js"
  },
  "standard": {
    "ignore": [
      "index.js"
    ]
  }
}
